# Architecture diagram

`system-architecture.png` is embedded in the root README.

Keep the editable source beside it — `system-architecture.mmd` if it is Mermaid,
or the export from whatever tool drew it — so the diagram can be corrected
without redrawing it.
