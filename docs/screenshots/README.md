# Product screenshots

Drop PNGs here and they render in the root README's screenshot section.

Expected names (the README links these):

| file | what to capture |
| --- | --- |
| `landing.png` | the marketing page |
| `dashboard.png` | `/app` — KPI tiles and the recovery funnel |
| `cases.png` | `/app/cases` — the list, showing the uplift column |
| `case-detail.png` | a case with the agent timeline expanded |
| `causal-dag.png` | the `diagnose` step, root cause and alternatives |
| `bandit.png` | the `decide` step, arms fan with confidence |
| `playbook.png` | `/app/playbooks/<slug>` — the learned arm table |
| `batch.png` | `/app/batch` — the learning curve |
| `roi.png` | `/app/roi` — gross vs incremental |
| `network.png` | `/app/network` — heatmap with the live outage banner |
| `audit.png` | `/app/audit` — the decision log |
| `simulator.png` | `/app/dev/simulator` — scenario runner |

Full-window captures at 1440×900 keep the set consistent.
