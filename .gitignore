# ---------- Node / Next.js ----------
node_modules/
.next/
out/
dist/
build/
.turbo/
*.tsbuildinfo
next-env.d.ts

# ---------- Python ----------
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.coverage
htmlcov/

# ---------- Env ----------
.env
.env.local
.env.*.local
!.env.example

# ---------- Editors ----------
.vscode/*
!.vscode/extensions.json
.idea/

# ---------- OS ----------
.DS_Store
Thumbs.db

# ---------- Logs ----------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---------- Supabase ----------
supabase/.branches/
supabase/.temp/

# Upstream clone of the apple-design skill (has its own .git)
.apple-design-skill/

# ---------- Demo seeding ----------
# Written by backend/scripts/prepare_demo.py. Regenerable, and it carries the
# demo account password in plaintext — the Playwright runner reads it from the
# working tree rather than from git.
backend/scripts/demo_state.json

# ---------- Demo recording ----------
# Playwright writes a ~25MB .webm per run, and the mp4 render is ~15MB. Both are
# build artefacts of tests/e2e/demo-recording.spec.ts, which is the thing worth
# versioning.
frontend/tests/e2e/recordings/
frontend/test-results/
docs/demo-recording.mp4
